spring:
  datasource:
    url: jdbc:h2:mem:contacts;MODE=PostgreSQL
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  flyway:
    url: jdbc:h2:mem:contacts;MODE=PostgreSQL
    user: sa
    password: sa
